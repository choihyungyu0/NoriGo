class AppConstants {
  const AppConstants._();

  static const appName = 'NoriGo';
  static const tagline = 'Travel smart, feel local.';
  static const fallbackError =
      'Something took longer than expected. NoriGo is showing a safe local fallback.';
}
